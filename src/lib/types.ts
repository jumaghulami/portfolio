export interface ScheduleItem {
  id: number;
  title: string;
  time: string;
}

export interface Note {
  id: number;
  text: string;
  editing?: boolean;
}
export interface Note {
  id: number;
  text: string;
  editing?: boolean;
}
